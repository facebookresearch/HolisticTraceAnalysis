[tool.black]
line-length = 120
exclude = '''
/(
    \.git
  | \.mypy_cache
  | \.pytest_cache
  | build
  | docs
  | stubs
)/
'''

[tool.isort]
line_length = 120
multi_line_output = 3
include_trailing_comma = true
use_parentheses = true
src_paths = ["hta", "tests"]
skip_glob = ["examples/*"]
